{
  "name": "ktrek-backed-database",
  "version": "0.1.0",
  "description": "",
  "main": "",
  "scripts": {
    "start": "npm run stop && docker network create ktrek && docker compose up --build -d && sleep 5 && npx prisma db push",
    "stop": "docker compose stop && docker network rm ktrek",
    "remove": "docker compose down -v --remove-orphans && docker network rm ktrek"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "prisma": "^5.17.0"
  },
  "dependencies": {
    "@prisma/client": "^5.17.0"
  }
}
